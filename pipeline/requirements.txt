# Core (always required)
httpx>=0.28.0              # HTTP client for OpenRouter + DuckDuckGo search
pydantic>=2.10.0           # Schema validation
pyyaml>=6.0.2              # Config parsing
python-dotenv>=1.0.1       # .env loading
tenacity>=9.0.0            # Retry logic

# Browser scraping
playwright>=1.49.0         # Headless browser for JS-rendered pages

# Database
supabase>=2.10.0           # Supabase client

# Data export
openpyxl>=3.1.5            # Excel export

# Optional: only needed if using Anthropic directly (premium tier)
# When using OpenRouter, anthropic is NOT required
anthropic>=0.40.0          # Claude API (optional — only for premium fallback)
